lint:
  files_exist:
    - .github/workflows/linting_comment.yml
  files_unchanged:
    - lib/NfcoreTemplate.groovy
    - .github/PULL_REQUEST_TEMPLATE.md
    - .github/workflows/branch.yml
    - .github/workflows/linting.yml
  nextflow_config: false # TODO: Remove when tools supports parameter types
  schema_params: false # TODO: Remove when tools supports parameter types
  nf_test_content:
    - tests/test_checkm_download_url_validation.nf.test # uses -preview, which runs no process at all, so there's no versions.yml to snapshot
nf_core_version: 4.1.0
repository_type: pipeline
template:
  author: "Hadrien Gourlé, Daniel Straub, Sabrina Krakau, James A. Fellows Yates, Maxime Borry"
  description: Assembly, binning and annotation of metagenomes
  force: false
  is_nfcore: true
  name: mag
  org: nf-core
  outdir: .
  version: 5.6.0dev
